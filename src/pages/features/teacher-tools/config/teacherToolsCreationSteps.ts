/** Top-level create flow steps shared across Quiz, Assignment, Worksheet, Exam */
export const TEACHER_TOOLS_CREATION_STEPS = ['Configure & generate', 'Review & publish'] as const

export const EXAM_CREATION_STEPS = ['Configure & generate', 'Review & schedule'] as const
