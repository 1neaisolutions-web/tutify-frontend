/**
 * Headline tokens mirrored from TeachingIntro BlurText (slide 1).
 * Slides 2–3 import this only — do not change slide 1 files.
 */
export const INTRO_HEADLINE = {
  fontSize: 96,
  fontWeight: 500,
  letterSpacing: '-0.03em',
  lineHeight: 1.1,
  wordGap: '0.42em',
  maxWidth: 1600,
  paddingX: 64,
} as const

/** Emphasis word (e.g. OVERWHELMED) — same size, heavier weight */
export const INTRO_HEADLINE_EMPHASIS_WEIGHT = 700
